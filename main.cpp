// #include "vector.hpp"
#include <iostream>
#include <vector>

int main(void) {
  //  std::vector<int> vector(10, 4);
  //
  //  for (std::vector<int>::iterator it = vector.begin(); it != vector.end();
  //  it++)
  //    std::cout << *it << std::endl;
  //  std::cout << vector.max_size() << std::endl;
  //  std::cout << vector.capacity() << std::endl;
  //  std::cout << vector.size() << std::endl;
  //  ft::vector<int> vector(10, 4);
  //
  //  vector.print_all();
  std::vector<int> test;

  std::cout << typeid(std::vector<int>::iterator).name() << std::endl;
}
